{% extends 'base.html.twig' %}

{% block title %} Modifier mon mot de passe - Nargui Five 93  {% endblock %}

{% block body %}

<div>
    <h1> Modification du mot de passe mot de passe </h1>
    <p>
        Dans cet espace vous allez pouvoir modifier votre mot de passe.
    </p>
    <hr>

    <section class="mt-0">
        {% if notification %}
            <div class="alert alert-info"> {{ notification }} </div>
        {% endif %}

        {{ form_start(form) }}
        {{ form_rest(form) }}
            <button class="btn btn-primary btn-md" type="submit"> Mettre à jour </button>
        {{ form_end(form) }}
    </section>

</div>
{% endblock %}
